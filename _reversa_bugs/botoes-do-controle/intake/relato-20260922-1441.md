# Relato de 2026-09-22 14:41 (contexto: botoes-do-controle)

> Escrivão: /reversa-debugger. Palavras do usuário entre aspas; o restante são observações do agente.

## Problema 1: botões de face geram mais de um acionamento numa única pressão

**Palavras do usuário:** "Está havendo um bug nas teclas X, quadrado e triângulo. Quando se segura, por exemplo, o triângulo e o aperto um pouquinho a mais, ele gera mais de um clique."

**Observações do agente (colhidas antes de perguntar):**

- Controle da sessão em curso: `DUALSHOCK 4 Wireless Controller` por Bluetooth, classificado `dualShock4` (GameSir G8+ em modo PlayStation, feature 012, instalada hoje). Fonte: `~/Library/Logs/joystick-ai/poc-20260922-143211.jsonl`, evento `controller.connected`.
- No log do app, o △ mostra rajadas de `input.button` down/up com duração de 15 ms e intervalo de 30 ms, cada `down` seguido de `shortcut.triggered { type: text }`: três "CONTINUAR" digitados em 120 ms (14:34:54.677 a .797). Ver `log-app-2026-09-22-rajadas-botoes-de-face.jsonl`.
- O mesmo padrão aparece em ✕, ○ e □ hoje (□ com 210 intervalos abaixo de 120 ms, cadência de 60 ms por vários segundos, incompatível com pressões humanas distintas). Ver `analise-intervalos-botoes-de-face.md`. O ○ não foi citado pelo usuário, mas o log o inclui.
- Nos logs anteriores com o Ipega (2026-09-20 e 2026-09-21) não há esse padrão: gaps mínimos de 30 ms só no □, que tem `repeat: true` e admite pressões rápidas deliberadas.
- A sonda da feature 012 (`sondas/probe-ds4-2026-09-22.log`, rodada sem o app, só GameController e IOHIDManager) já registrava o fenômeno às 14:22:56: numa pressão única de △, o bit 7 do byte 7 do relatório `0x11` alternou 0x08→0x88→0x08 três vezes a cada 30 ms, e o `GCControllerButtonInput` relatou PRESSIONADO/solto em sincronia. Na P-01, em que cada botão foi pressionado uma vez, A/B/X/Y, Options e Menu registraram de 3 a 6 pressões; L1, R1, L3 e R3 registraram uma. Ver `sonda-012-p01-p02-oscilacao-triangulo.log`.
- Conclusão preliminar (hipótese, não diagnóstico): as bordas repetidas já chegam no relatório HID do controle; o app as repassa fielmente porque `ButtonReader` entrega toda mudança de `pressedChangedHandler` e a spec (RN-EC-06) manda gerar evento a cada mudança de estado, sem qualquer filtro temporal. Ações que agem no pressionar (`text`, `openPalette`, acorde sem repetição) disparam uma vez por borda.
- Configuração vigente (`~/.config/joystick-ai/config.json`, camada base): ✕ = Return, □ = Delete com `repeat: true`, △ = texto "CONTINUAR", ○ = Escape. Portanto, o "clique" relatado é a ação de teclado disparada mais de uma vez.

**Faltou perguntar:** em quais controles o usuário observou (só no GameSir em modo DS4, ou também no DualSense e no Ipega); se "aperto um pouquinho a mais" é pressão maior ou tempo maior; severidade e prioridade; closure policy do registro (primeira execução).

## Evidências nesta pasta

- `log-app-2026-09-22-rajadas-botoes-de-face.jsonl`: extrato do log do app, janela 14:34:40 a 14:35:00, só botões de face e atalhos disparados
- `analise-intervalos-botoes-de-face.md`: intervalos entre pressões nos três logs mais recentes (Ipega x2, DualShock 4)
- `sonda-012-p01-p02-oscilacao-triangulo.log`: extrato da sonda da feature 012 com o relatório HID bruto oscilando

## Respostas do usuário (14:44)

- Controles em que observou: só no GameSir G8+ em modo DualShock 4.
- Severidade: high. Prioridade: P1.
- Closure policy do registro (primeira execução): software local.

## Confirmação no hardware (2026-09-22, depois do fix)

Usuário: "Funcionou." App corrigido instalado às 15:10 (log `poc-20260922-151037.jsonl`), GameSir G8+ em modo DualShock 4: uma pressão do △ passou a gerar um único acionamento. RF-EC-11 do adendo `bug-BUG-20260922-33HN-v001.md` conferida. O bug `BUG-20260922-33HN` segue travado com `DONE.md`.
